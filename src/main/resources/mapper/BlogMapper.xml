<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jdktomcat.mybatis.example.mapper.BlogMapper">
    <!-- 二级缓存 -->
    <cache/>
    <select id="selectBlog" resultType="com.jdktomcat.mybatis.example.entry.Blog">
        select * from blog where id = #{id}
    </select>
</mapper>